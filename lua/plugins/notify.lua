return {
  "rcarriga/nvim-notify",
  opts = {
    --- @usage Animation style one of { "fade", "slide", "fade_in_slide_out", "static" }
    stages = "slide",
    --- @usage timeout for notification in ms, default 5000
    timeout = 2000,
    --- @usage User render fps value
    fps = 24,
  },
}
